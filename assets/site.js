document.addEventListener("click",async function(e){const b=e.target.closest("[data-copy-target]");if(!b)return;const id=b.getAttribute("data-copy-target");const el=document.getElementById(id);if(!el)return;const text=el.innerText;try{await navigator.clipboard.writeText(text);const old=b.textContent;b.textContent="MODELO COPIADO";setTimeout(()=>b.textContent=old,1800)}catch(err){const r=document.createRange();r.selectNodeContents(el);const s=window.getSelection();s.removeAllRanges();s.addRange(r);b.textContent="SELECIONADO PARA COPIAR"}});
